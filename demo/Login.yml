info:
  name: Login
  type: http
  seq: 2

http:
  method: POST
  url: "{{URL}}/intranet/api/v1/authentications/login"
  body:
    type: json
    data: |-
      {
        "email": "alexander@gmail.comdddd",
        "password": "Test*123456"
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
