info:
  name: Get countries
  type: http
  seq: 1

http:
  method: GET
  url: http://localhost:8080/intranet/api/v1/countries
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
